"use client";
import React from "react";
import PledgeFormsScreen from "./screen";

const WrappedPledgeForm = (props: any) => {
  return <PledgeFormsScreen {...props} />;
};

export default WrappedPledgeForm;
